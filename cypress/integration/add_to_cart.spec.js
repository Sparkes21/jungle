describe('jungle homepage', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000/')
  });
  it("Should display the homepage", () => {});
  
  it("Should change the count of the cart button when adding products to it", () => {
  });
});
